version: '3.8'

# ================================================================
# UNIVERSAL PRE-INIT SERVICE (runs the auto-config script)
# ================================================================
services:
  pre-init:
    image: bash:latest
    volumes:
      - .:/app
    working_dir: /app
    command: ["bash", "-c", "chmod +x ./auto-config.sh && ./auto-config.sh"]
    # This ensures the script runs before anything else
    restart: "no"

  # ======================
  # FRONTEND SERVICE
  # ======================
  frontend:
    build: ./Frontend
    ports:
      - "3000:8080"
    networks:
      - xencrypt-network
    depends_on:
      pre-init:
        condition: service_completed_successfully
      crypto-scanner:
        condition: service_started
      db-service:
        condition: service_started
    restart: unless-stopped

  # ======================
  # CRYPTO SCANNER SERVICE
  # ======================
  crypto-scanner:
    build:
      context: ./scan-service
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./scan-service:/app
    environment:
      - PYTHONUNBUFFERED=1
      - DB_SERVICE_URL=http://db-service:8001
    networks:
      - xencrypt-network
    depends_on:
      pre-init:
        condition: service_completed_successfully
      db-service:
        condition: service_started
      postgres:
        condition: service_healthy
    restart: unless-stopped

  # ======================
  # DATABASE SERVICE
  # ======================
  db-service:
    build:
      context: ./db-service
      dockerfile: Dockerfile
    ports:
      - "8001:8001"
    volumes:
      - ./db-service:/app
    environment:
      - PYTHONUNBUFFERED=1
      - DATABASE_URL=postgresql://scanuser:scanpass@postgres:5432/scandb
    networks:
      - xencrypt-network
    depends_on:
      pre-init:
        condition: service_completed_successfully
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8001/health"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 10s

  # ======================
  # POSTGRES DATABASE
  # ======================
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: scanuser
      POSTGRES_PASSWORD: scanpass
      POSTGRES_DB: scandb
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./postgres-init:/docker-entrypoint-initdb.d
    networks:
      - xencrypt-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U scanuser -d scandb"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  # ======================
  # ADMINER (DB WEB UI)
  # ======================
  adminer:
    image: adminer
    restart: always
    ports:
      - "8080:8080"
    networks:
      - xencrypt-network

  # =============================================================================
  # CRYPTO AUDIT SYSTEM SERVICES (inside system-scaner)
  # =============================================================================

  # ---------------------------------------------------------------------------
  # CENTRAL API SERVER
  # ---------------------------------------------------------------------------
  system-scan:
    build:
      context: ./system-scaner
      dockerfile: Dockerfile
    container_name: system-scan
    ports:
      - "9000:9000"
    volumes:
      - server-data:/app/data
    environment:
      - PYTHONUNBUFFERED=1
    networks:
      - xencrypt-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/api/v1/admin/stats"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ---------------------------------------------------------------------------
  # LINUX AGENT #1 - MODERN CONFIGURATION
  # ---------------------------------------------------------------------------
  linux-agent-modern:
    build:
      context: ./system-scaner/agents/linux
      dockerfile: Dockerfile.modern
      args:
        API_SERVER: http://system-scan:9000
    container_name: linux-agent-modern
    hostname: ubuntu-modern-agent
    depends_on:
      pre-init:
        condition: service_completed_successfully
      system-scan:
        condition: service_started
    environment:
      - AGENT_PROFILE=modern
      - API_BASE_URL=http://system-scan:9000
      - POLL_INTERVAL=10
      - AGENT_NAME=ubuntu-modern-agent
    networks:
      - xencrypt-network
    restart: unless-stopped
    privileged: false
    cap_add:
      - SYS_ADMIN
    volumes:
      - agent-modern-data:/var/lib/crypto_agent

  # ---------------------------------------------------------------------------
  # LINUX AGENT #2 - LEGACY CONFIGURATION
  # ---------------------------------------------------------------------------
  linux-agent-legacy:
    build:
      context: ./system-scaner/agents/linux
      dockerfile: Dockerfile.legacy
      args:
        API_SERVER: http://system-scan:9000
    container_name: linux-agent-legacy
    hostname: debian-legacy-agent
    depends_on:
      pre-init:
        condition: service_completed_successfully
      system-scan:
        condition: service_started
    environment:
      - AGENT_PROFILE=legacy
      - API_BASE_URL=http://system-scan:9000
      - POLL_INTERVAL=10
      - AGENT_NAME=debian-legacy-agent
    networks:
      - xencrypt-network
    restart: unless-stopped
    privileged: false
    cap_add:
      - SYS_ADMIN
    volumes:
      - agent-legacy-data:/var/lib/crypto_agent

  # ---------------------------------------------------------------------------
  # LINUX AGENT #3 - MIXED CONFIGURATION
  # ---------------------------------------------------------------------------
  linux-agent-mixed:
    build:
      context: ./system-scaner/agents/linux
      dockerfile: Dockerfile.mixed
      args:
        API_SERVER: http://system-scan:9000
    container_name: linux-agent-mixed
    hostname: alpine-mixed-agent
    depends_on:
      pre-init:
        condition: service_completed_successfully
      system-scan:
        condition: service_started
    environment:
      - AGENT_PROFILE=mixed
      - API_BASE_URL=http://system-scan:9000
      - POLL_INTERVAL=10
      - AGENT_NAME=alpine-mixed-agent
    networks:
      - xencrypt-network
    restart: unless-stopped
    privileged: false
    cap_add:
      - SYS_ADMIN
    volumes:
      - agent-mixed-data:/var/lib/crypto_agent

# ======================
# NETWORKS
# ======================
networks:
  xencrypt-network:
    driver: bridge
    name: xencrypt-network

# ======================
# VOLUMES
# ======================
volumes:
  postgres-data:
    driver: local
  server-data:
    driver: local
  agent-modern-data:
    driver: local
  agent-legacy-data:
    driver: local
  agent-mixed-data:
    driver: local
